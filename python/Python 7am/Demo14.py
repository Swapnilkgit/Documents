# Example program to read input from user.
# The input function will read input from user
# the input function will read any input and it will return in String type only.

name = input("Enter Name ")
print("Welcome Mr/Miss :",name)
print(type(name))

